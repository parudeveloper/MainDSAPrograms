package com.maindsaprograms.class_18th_06_2024

object DbConnection {
   /* val dbCon = Database();

    fun getRetrofit(baseUrl): Retrofit {
        val r = Retrofit.Builder().apply {
            baseUrl(baseUrl)
            addConvertorFactory(GsonConvertorFactory.create())
        }.build()

        return r
    }*/
}